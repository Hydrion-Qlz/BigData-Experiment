<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xjtu.xjtuse.assistance.user.dao.UserDao">
    <select id="getUserInfo" resultType="com.xjtu.xjtuse.assistance.user.dto.UserInfo">
        select *
        from t_user
        where email = #{email}
    </select>

    <update id="updateUserInfo">
        update t_user
        set username = #{userInfo.username},
            age      = #{userInfo.age},
            gender   = #{userInfo.gender},
            birthday = #{userInfo.birthday},
            hobbies  = #{userInfo.hobbies}
        where id = #{userInfo.id}
    </update>

    <select id="getMessageList" resultType="com.xjtu.xjtuse.assistance.user.dto.Message">
        select *
        from t_message
        where u_id = #{userId}
    </select>

    <update id="changePwd">
        update t_user
        set password = #{password}
        where email = #{email}
    </update>

    <select id="getSearchHistory" resultType="com.xjtu.xjtuse.assistance.user.dto.SearchHistoryItem">
        select id,
               u_id       as userId,
               web_url    as webUrl,
               start_time as startTime,
               percentage
        from t_search_history
        where u_id = #{userId}
    </select>
</mapper>
